# portfolio
Portfolio Website

Latest deployed file: `index.html` (synced from `michael-leng-timeline-v4 (8).html`).

## Vercel deployment notes
- `vercel.json` is configured to serve existing files first and rewrite any unmatched route to `index.html`.
- This prevents Vercel `404: NOT_FOUND` on direct visits to non-root URLs.


Deployment setup steps: see `DEPLOY.md`.
